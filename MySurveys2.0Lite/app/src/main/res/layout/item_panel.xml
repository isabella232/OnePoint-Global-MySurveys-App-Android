<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_view_panel"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    style="@style/item_view_panel_style">
    <RelativeLayout
        android:id="@+id/image_logo_container"
        style="@style/panel_image_container_style">

        <ImageView
            android:id="@+id/img_logo_panel"
            style="@style/panel_logo_image_style"/>

        <ProgressBar
            android:id="@+id/progress_panel_bg"
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginRight="25dp"
            android:layout_toLeftOf="@id/img_logo_panel"
            android:visibility="invisible"/>

        <ProgressBar
            android:id="@+id/progress_panel"
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:visibility="invisible"/>

    </RelativeLayout>

    <TextView
        android:id="@+id/tv_panel_name"
        style="@style/tv_panel_name_style"
        android:layout_below="@id/image_logo_container"/>

</RelativeLayout>